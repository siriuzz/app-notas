version: '3'
services:
  web:
    build: .
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: npm run dev
    links:
      - mongo
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - "27017:27017"
